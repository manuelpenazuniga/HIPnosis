# Python
__pycache__/
*.pyc
.venv/
venv/
# Env / secretos
.env
*.env
!.env.example
# OS
.DS_Store

# Docs (brainstorm, workflow interno, auditorías) — no forman parte de la entrega
docs/
# Workspaces del pipeline (repos objetivo por run)
workspaces/
# Bus efímero del orquestador
.agents/ARCHITECT_INPUT.md
.agents/ARCHITECT_ANSWER.md
# Worktrees de tareas
../HIPnosis-t*
# Workspaces del pipeline dentro de orchestrator
orchestrator/workspaces/

# editable install metadata
*.egg-info/
__pycache__/

# Tooling local (Claude Code / skills descargadas) — no es parte del entregable
.claude/
.agents/
skills-lock.json

# Bitácoras internas del equipo (routing de workers, intel competitiva, checkpoints)
# — proceso, no producto; opsec vs rivales
DEVIATIONS.md
ESTADO.md
BITACORA.md
