# HIPnosis — Port Certificate

> **Repo:** `github.com/zjin-lcf/HeCBench (src/bsw-cuda)`
> **Run:** `run_bsw01a2`  ·  **Generated:** `2026-07-08`
> **Difficulty:** `medium`  ·  **Build system:** `make`

---

## 1. Executive summary

HIPnosis ported bsw-cuda (Smith-Waterman) from CUDA to ROCm/HIP autonomously: 8 compile errors resolved in 4 iterations, 100% locally (Gemma 3 27B + deterministic rules, $0 in API), with 2 critical wavefront-64 corrections a textual port would have missed. The benchmark verifies PASS against its internal reference.

---

## 2. Inventory

| Metric | Value |
|---|---|
| CUDA files (`.cu`/`.cuh`) | **2** |
| Kernel lines (LOC) | **788** |
| Build system | `make` |
| Difficulty (heuristic §5.3) | `medium` |
| NVIDIA libraries detected | 0: _none_ |

### 2.1 CUDA API calls (count)

| API | Count |
|---|---:|
| `cudaMalloc` | 6 |
| `cudaMemcpy` | 10 |
| `cudaFree` | 6 |
| `cudaDeviceSynchronize` | 2 |

---

## 3. Applied fixes

| Class | n | Tier | Commits |
|---|---:|---|---|
| `E01` | 3 | `deterministic` | `c1a2b3d` |
| `E02` | 3 | `deterministic` | `e5f6a71` |
| `E05` | 2 | `local` | `f6a7b82` |

---

## 4. Wave64 findings (deterministic linter, §5.2)

| file:line | Pattern | Severity | Explanation |
|---|---|---|---|
| `kernel.cu:13` | `W01` | `correctness` | 32-bit mask — on wave64 the mask/result are 64-bit |
| `kernel.cu:13` | `W02` | `correctness` | Ballot result truncated to 32 bits on wave64 |
| `kernel.cu:54` | `W05` | `suspicious` | Lane arithmetic assumes a 32-wide warp (&31, >>5) |
| `kernel.cu:278` | `W04` | `suspicious` | Hardcoded width 32 — AMD wavefront is 64 |

---

## 5. Verification

- **Verdict:** `PASS`
- **Detail:** benchmark self-check (scores identical to the internal CPU reference)
- **Tolerances:** default rtol/atol (F-09); floats are never compared exactly.

---

## 6. Timing

```json
{
  "kernel_ms": 12.5
}
```

---

## 7. Limitations and NEEDS_HUMAN

> ⚠️ **Mandatory section** (honest degradation, INV-5). Even when empty, the
> certificate includes it so the reader knows the orchestrator does NOT hide
> what it could not resolve.

_No unresolved groups. The loop converged (or did not reach the fix phase)._

---

## 8. Efficiency metrics

- **% local fixes (deterministic + local)**: **100%** (6 deterministic + 2 local / 8 total)
- **Local tokens:** 438
- **Remote tokens:** 0
- **Loop iterations:** 4
- **Initial → current errors:** 8 → 0

> The numbers in this section are computed from the pipeline counters
> (F-17/INV-7): no field is written by an LLM.

---

_Generated by HIPnosis · F-17/INV-7: numbers always from code._
